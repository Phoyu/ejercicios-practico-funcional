Ejercicio 2

> data Semaforo = Rojo | Verde | Amarillo
>   deriving(Show)

Se debe indicar que el dato algebraico es una instancia de la clase Show, con deriving se le dice al compilador que la derive automaticamente.
